# TDDD 2026 Edition 書き直し TODO

## 前提・境界

- 変更対象は `README.ja.md`, `README.md`, `docs/ja/`, `docs/en/` 以下のMarkdownファイルのみ
- `.gitignore` など設定ファイルは変更しない
- 既存の文章トーン（簡潔・断定的）は維持する
- 日本語版と英語版は内容を一致させる

---

## 0. タグを切る（Claudeではなく手動で行う）

- [ ] `git tag v2026-vscode` を実行して現状を保存する
- [ ] `git push origin v2026-vscode` でリモートにも反映する

---

## 1. README.ja.md / README.md

- [ ] 冒頭の「これは何か」セクションの直後に以下を追加する（日本語版）

```
> このドキュメントはWebページ制作のチュートリアルではありません。
> TDDDは「判断をTODOに残す」ための開発運転方法です。
```

- [ ] 英語版にも同様の一文を追加する

---

## 2. docs/ja/TDDD.md / docs/en/TDDD.md

- [ ] 「年次性について」セクションの前提を書き直す
  - 削除：「対話型AIと自動生成AIが分離している前提」という記述
  - 追加：「同一ツール内でも、設計モード（判断）と実装モード（実行）を意識的に切り替えることがTDDDの前提」という記述

- [ ] Loop A / Loop B の説明を書き直す
  - 削除：「対話型AI」「自動生成AI」という**ツール名**での役割定義
  - 変更：「判断をするフェーズ（Loop A）」「実行するフェーズ（Loop B）」という**モード**での定義に統一
  - 補足として「Claude CodeやCopilot Agentなど、1つのツールが両方を担う場合も、フェーズを意識的に分けることが重要」と追記する

---

## 3. docs/ja/simple.md / docs/en/simple.md

- [ ] 「前提」セクションを書き直す
  - 削除：「Pythonが少し書ける」
  - 変更後の前提：
    - 何を作るかは大体決まっている
    - 設計や分割が苦手
    - 生成AIを使っている（ChatGPT / Claude / Copilot等）
    - VSCodeを使っている（HTMLやPythonが書けなくても構わない）

- [ ] Step 3「TODO を実行させる」の例示を変更する
  - 削除：「Pythonで実装してください」という例
  - 変更：「HTMLとCSSでWebページを作る」または「指定した言語で実装する」という汎用的な例に変更する
  - 補足：「言語やツールはTODOに明示する」と追記する

---

## 4. docs/ja/advanced.md / docs/en/advanced.md

- [ ] 「応用編で前提にする2つの理由」の「手段の理由」を書き直す
  - 削除：「対話型生成AIと自動生成AIには、まだ明確な役割差がある」
  - 変更：「判断フェーズと実行フェーズは、同一ツールを使う場合でも意識的に分離する必要がある」

- [ ] 本文中の「対話型AI」「自動生成AI」という表現を全箇所確認し、文脈に応じて以下のいずれかに置き換える
  - 「判断フェーズのAI操作」
  - 「実行フェーズのAI操作」
  - または具体例として「（例：Claude Codeの設計モード）」を補足として追記する

---

## 完了条件

- 日本語版・英語版の内容が一致している
- 「対話型AI」「自動生成AI」をツールとして固定する記述が残っていない
- 簡単編の対象読者にHTMLがわからない非エンジニアが含まれている
- READMEに「チュートリアルではない」旨が明記されている
